services:
  - type: worker
    name: telegram-bot
    runtime: python
    plan: free
    buildCommand: |
      pip install --upgrade pip
      pip install --only-binary=all -r requirements.txt
    startCommand: python bot.py
    publishDirectory: "."
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: WEBAPP_URL
        sync: false